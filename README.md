# informacion básica de mi primera aplicación node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     este argumento indica que tabla de multiplicar se mostra.
                                                             [number] [required]
  -l, --listar   este argumento indica si se mostrará la tabla de multiplicar
                 por pantalla o no.                   [boolean] [default: false]
  -h, --hasta    este argumento indica hasta que numero se multiplicará la
                 tabla.                                   [number] [default: 10]
```